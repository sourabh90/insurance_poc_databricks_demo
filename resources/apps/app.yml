resources:
  apps:
    insurance_poc_databricks_demo_app:
      name: "databricks-billing-usage-${bundle.target}"
      description: "Databricks Billing Usage App — interactive Streamlit dashboard for compute cost analytics"
      source_code_path: ../../src/app
      resources:
        - name: sql-warehouse
          sql_warehouse:
            id: ${var.warehouse_id}
            permission: CAN_USE
