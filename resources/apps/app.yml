resources:
  apps:
    insurance_poc_databricks_demo_app:
      name: "ins-claims-demo-${bundle.target}"
      description: "Interactive Databricks billing usage dashboard — Streamlit on Databricks Apps"
      source_code_path: ../../src/app
      resources:
        - name: sql-warehouse
          sql_warehouse:
            id: ${var.warehouse_id}
            permission: CAN_USE
