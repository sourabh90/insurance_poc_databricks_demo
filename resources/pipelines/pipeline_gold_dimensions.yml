resources:
  pipelines:
    insurance_poc_gold_dimensions:
      name: "[${bundle.target}] insurance_poc - Pipeline - Load Gold Dimensions"
      catalog: ${var.gold_catalog}
      schema:  dimensions
      libraries:
        - notebook:
            path: ../../src/pipelines/gold_dimensions.py
      configuration:
        silver_catalog: ${var.silver_catalog}
        silver_schema:  ${var.silver_schema}
      continuous: false
      serverless: true
